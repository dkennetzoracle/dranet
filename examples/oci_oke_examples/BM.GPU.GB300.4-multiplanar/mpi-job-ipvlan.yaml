# all_reduce_perf across 2 x BM.GPU.GB300.4 (8 ranks), over the multi-planar
# RoCEv2 fabric, using IPVLAN children of the rail PFs rather than moving the
# PFs into the Pods. Requires the RDMA subsystem in shared netns mode; see the
# README.
#
# NCCL_IB_GID_INDEX=7, not 3. The GID table is namespace-filtered: the Pod sees
# zeros where the host PF's four GIDs sit (0-3) and its own child's entries above
# them -- link-local at 4/5, the routable address at 6/7. 7 is the RoCE v2 global
# GID and it lands there uniformly on all four rails. Verify with:
#   cat /sys/class/infiniband/rdma_rail0/ports/1/gid_attrs/types/7   -> RoCE v2
#   cat /sys/class/infiniband/rdma_rail0/ports/1/gids/7
#
# NCCL_MNNVL_ENABLE=0 on purpose: these nodes share one NVLink clique, and with
# MNNVL enabled NCCL moves inter-node traffic over NVLink and never touches the
# rail NICs. Disabling it forces the NET/IB path, which is what exercises the
# autoconfigured addresses on the IPVLAN children.
#
# The four RDMA devices named here are the ones behind plane 0 of each rail
# (rdma_p0_rail0..3), which is what the `4rail-ipvlan` ResourceClaimTemplate
# selects. The exact-match form (`==`) matters in shared netns mode: the Pod can
# see every RDMA device on the node, but only the claimed rails have GIDs in its
# namespace.
apiVersion: kubeflow.org/v2beta1
kind: MPIJob
metadata:
  name: nccl-test-multiplanar-ipvlan
spec:
  slotsPerWorker: 4
  runPolicy:
    cleanPodPolicy: Running
  mpiReplicaSpecs:
    Launcher:
      replicas: 1
      template:
        spec:
          # The launcher only runs mpirun, but the image is arm64 and the GPU
          # nodes carry a NoSchedule taint, so it needs both to land on one.
          nodeSelector:
            kubernetes.io/arch: arm64
          tolerations:
          - key: "nvidia.com/gpu"
            operator: "Exists"
            effect: "NoSchedule"
          containers:
          - name: nccl
            image: iad.ocir.io/idxzjcdglx2s/nccl-tests:cuda-13.1.1-ubuntu-24.04-nccl-2.29.3-020926.1
            command: ["/bin/bash", "-c"]
            args:
            - |
              NUM_GPUS=4
              NUM_HOSTS=$(sed -n '$=' /etc/mpi/hostfile)
              NP=$(($NUM_HOSTS*$NUM_GPUS))
              while ! (for host in $(awk '{print $1}' /etc/mpi/hostfile); do
                ssh -o ConnectTimeout=5 -o StrictHostKeyChecking=no $host exit 2>/dev/null || exit 1
              done); do
                echo "Waiting for workers to be ready..."
                sleep 5
              done
              echo "All workers ready: $NUM_HOSTS nodes x $NUM_GPUS GPUs = $NP ranks"
              mpirun \
                --allow-run-as-root \
                --bind-to none \
                --mca pml ucx \
                --mca coll ^hcoll \
                --mca btl_tcp_if_include eth0 \
                -x LD_LIBRARY_PATH \
                -x UCX_TLS=tcp \
                -x UCX_NET_DEVICES=eth0 \
                -x NCCL_DEBUG=INFO \
                -x NCCL_DEBUG_SUBSYS=INIT,NET \
                -x NCCL_SOCKET_IFNAME=eth0 \
                -x NCCL_MNNVL_ENABLE=0 \
                -x NCCL_CUMEM_ENABLE=1 \
                -x NCCL_IB_HCA==rdma_rail0,rdma_rail1,rdma_rail2,rdma_rail3 \
                -x NCCL_NET_GDR_C2C=1 \
                -x NCCL_IB_GID_INDEX=7 \
                -x NCCL_IB_TC=41 \
                -x NCCL_IB_SL=0 \
                -x NCCL_IB_TIMEOUT=22 \
                -x NCCL_IB_QPS_PER_CONNECTION=1 \
                -x NCCL_IB_SPLIT_DATA_ON_QPS=0 \
                -x NCCL_BUFFSIZE=16777216 \
                -x NCCL_DMABUF_ENABLE=1 \
                -x NCCL_IB_DATA_DIRECT=0 \
                -x NCCL_NET_PLUGIN=none \
                -x HCOLL_ENABLE_MCAST_ALL=0 \
                -x coll_hcoll_enable=0 \
                -np $NP \
                /workspace/nccl-tests/build/all_reduce_perf -b 8 -e 8G -f 2 -g 1 -c 0
    Worker:
      replicas: 2
      template:
        spec:
          affinity:
            podAntiAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
              - labelSelector:
                  matchLabels:
                    training.kubeflow.org/job-name: nccl-test-multiplanar-ipvlan-ipvlan
                    training.kubeflow.org/job-role: worker
                topologyKey: kubernetes.io/hostname
          automountServiceAccountToken: false
          volumes:
          - name: shm
            emptyDir:
              medium: Memory
              sizeLimit: 32Gi
          resourceClaims:
          - name: rail-nics
            resourceClaimTemplateName: 4rail-ipvlan
          containers:
          - name: nccl
            image: iad.ocir.io/idxzjcdglx2s/nccl-tests:cuda-13.1.1-ubuntu-24.04-nccl-2.29.3-020926.1
            volumeMounts:
            - mountPath: /dev/shm
              name: shm
            resources:
              limits:
                nvidia.com/gpu: 4
              claims:
              - name: rail-nics
            securityContext:
              capabilities:
                add:
                - IPC_LOCK
          tolerations:
          - key: "nvidia.com/gpu"
            operator: "Exists"
            effect: "NoSchedule"
